version: '3.8'

services:
  hapio-portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hapio-portal-app
    ports:
      - "8081:80"
    networks:
      - agendamiento_default
    restart: always

networks:
  agendamiento_default:
    external: true
